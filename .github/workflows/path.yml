name: CI Pipeline (Windows)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Print Python Details
        run: |
          echo "🔹 Python Version:"
          python --version
          echo "🔹 Installed Packages:"
          pip list

      - name: Print Pip Cache Directory
        run: |
          echo "🔹 Pip cache directory:"
          python -c "import os; print(os.path.expanduser(os.path.join('~', '.cache', 'pip')))"

      - name: Print Python User Base Directory
        run: |
          echo "🔹 User base directory:"
          python -c "import sysconfig; print(sysconfig.get_path('purelib'))"

      - name: Print Environment Variables
        shell: pwsh
        run: |
          echo "🔹 Environment Variables:"
          Get-ChildItem Env:

      - name: 🔍 Check Playwright Installation
        shell: pwsh
        run: |
          echo "🔹 Checking Playwright installation..."
          if (Get-Command playwright -ErrorAction SilentlyContinue) {
            Write-Host "✅ Playwright is installed."
          } else {
            Write-Host "⚠️ Playwright is not installed!"
          }

      - name: 🔍 Print Playwright Cache Path
        shell: pwsh
        run: |
          echo "🔹 Checking Playwright cache directory..."
          $playwrightCache = "$env:USERPROFILE\AppData\Local\ms-playwright"
          if (Test-Path $playwrightCache) {
            Write-Host "✅ Playwright cache found at: $playwrightCache"
            Get-ChildItem -Path $playwrightCache
          } else {
            Write-Host "⚠️ No Playwright cache directory found!"
          }

      - name: Run Tests
        run: pytest
